# This is the build properties file

# Project properties
project.bin=${basedir}/bin
project.src=${basedir}/src
project.outputfile=${project.bin}/vdp3.swf
project.asfile=${basedir}/src/vdp3.as
project.configfile=${basedir}/project-config.xml